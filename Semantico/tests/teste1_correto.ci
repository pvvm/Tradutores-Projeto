// Funcao que imprime a lista
int oi;

int _write_list(int list lista, int tamanho) {
    int i;
    float header;
    for(i = 0.0; i < tamanho; i = i + 1) {
        header = ?lista;    // Testando comentario
        lista = %lista;
        write(header);
        write(" ");
    }
    return 1;
}

// Funcao que soma e subtrai 5

int soma(float a, float b) {
    return a+b*5;
}

/*float soma() {
    return 1.0;
}*/

/*
Testando comentario multilinha
*/
int main() {
    writeln("Digite dois numeros: ");
    int a;
    int b;
    int lista;
    {
        int testando;
        testando = 1 + 3.0 - a + b;
    }
    read(a);
    read(b);
    int r;
    {
        int testando2;
        testando2 = r + 1;
    }
    r = soma(a, b, s, c, s);

    if(r > 0) {
        int list lista;
        lista = NIL;
        int i;
        int num;
        float total;
        int tamanho;
        total = 0;
        tamanho = 0;

        for(i = 0; i < r; i = i + 1){
            writeln("Digite um numero para compor a lista: ");
            read(num);
            lista = num : lista;
            total = num + total;
            tamanho = 1 + tamanho;
        }
        write("A soma dos elementos da lista eh ");
        if(total > -999999 && total < 0 && sei_la == oi) {
            writeln("negativa. Como pode ver a lista:");
            _write_list(lista, tamanho);
        } else if (total > 99999 * 23892 + 21 + 92 * (32 - - 12)) {
            writeln("Isso eh so um teste pra ver se funciona o else if");
        } else if (-999999.12 * -21.3 + (248.2 - 21) - 21.1 > total && total < 0) {
            writeln("Outro teste de else if");
        } else {
            writeln("positiva. Como pode ver a lista:");
            _write_list(lista, tamanho);
        }
    } else 
        writeln("AQUI");
    
    int teste1;
    int teste2;
    a = teste1 * teste2 + 9.9 * (2);

    //oi = 2 + oi +e_eu_sei_la(lista, tamanho);
    
    lista = 5;
    oi = 50 - 20 + (1 && 1) * 20;

    float list t;
    float list w;
    t = w;
    a = b + ?t;
    int i;
    int oi;
    float v;



    i = v * soma(a, b);

    soma2(2);

    _write_list(3, 2.3);

    soma(3, 2.0);

    return 0;
}