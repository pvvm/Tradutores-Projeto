int list func(int list b){
    return 2*b;
}
int main() {
    b = 1; // erro semântico, referência indefinida para a variável
    int a;
    float a; // erro semântico, redefinição de variável ou função
    int list c;
    func(c); // erro semântico, impossível converter tipo list(int) para float
}