float menor_numero(float list lista, int num) {
    int i;
    int numero;
    for(i = 0; i < num; i = i + 1) {
        if(numero < ?lista) {
            numero = ?lista;
        }
        lista1 = %lista;
    }
    &
    return numero;
}

int main() {
    writeln("Digite um numero inteiro positivo: ");
    num = 0;
    int num;$
    i = i + 1;
    numero = 1.0 + 1 - (20 / 20 + 34.3 * 0);
    read(num);
    if(num >>> 0) {
        write("Digite ");
        write();
        writeln(" numeros reais:");
        int i;
        float numero;
        float list lista;

        for(i = 0) {
            read(numero);
            @
            lista = numero : lista;
        }

        write("O menor numero eh: ");
        int list aux;
        aux = menor_numero(lista, num);
        writeln(menor_numero(lista, lista));
        
    }
    funcao_que_nao_existe(3, 2);
    return 0;
}